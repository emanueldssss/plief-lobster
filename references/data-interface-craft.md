# Data interface craft

Dense data needs hierarchy, scan paths, sort/filter feedback, long values, zero/negative states, loading and empty states. Verify the data owner and scenario; a table screenshot alone does not prove the interaction.
